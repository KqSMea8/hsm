class MainActivity {
    constructor() {
        this.init()
    }

    init() {
        head.js([
            db.data.distDir + 'static/js/VueP.js' + time,
            db.data.distDir + 'admin/pages/Page.js' + time,
            db.data.distDir + 'admin/pages/destroy.js' + time
        ], function () {
            destroy = new destroy('index');
            setTimeout(() => {
                destroy.vue.init()
            }, 100);
        })
    }
}
